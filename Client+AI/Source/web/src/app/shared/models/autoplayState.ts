export class AutoplayState {
    /**
     *autoplay is on `true`
     * autoplay is off 'false
     *
     * @type {boolean}
     * @memberof AutoplayState
     */
    state: boolean;

    /**
     *autoplay switch tab interval in ms
     *
     * @type {number}
     * @memberof AutoplayState
     */
    interval: number;
}
